/**
 * login/page.tsx
 * Route: /login
 * Placeholder until auth is wired up. Or can just have as a pretend log in screen
 */
export default function LoginPage() {
  return (
    <section className="hero">
      <h2>Login</h2>
      <p>Login page rendered (placeholder).</p>
    </section>
  );
}
